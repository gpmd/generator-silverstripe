language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-silverstripe' ]; then cd .. && eval "mv $currentfolder generator-silverstripe" && cd generator-silverstripe; fi
